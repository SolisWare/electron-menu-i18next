{
  "name": "electron-menu-i18next-basic-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm run build:typescript && npm run check:javascript",
    "build:typescript": "node scripts/clean.cjs && tsc",
    "check:javascript": "node scripts/check-javascript.cjs",
    "start": "npm run start:typescript",
    "start:javascript": "npm run check:javascript && node scripts/launch.cjs javascript",
    "start:typescript": "npm run build:typescript && node scripts/launch.cjs typescript"
  },
  "dependencies": {
    "@solisware/electron-menu-i18next": "file:../..",
    "i18next": "^24.0.0"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "typescript": "^5.5.0"
  }
}
